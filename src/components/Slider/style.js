import styled from 'styled-components'

export const Container = styled.div`
  overflow: hidden;
  h2 {
    color: #ffffff;
    font-size: 28px;
    margin: 50px 20px 0;
  }

  .swiper-wrapper {
    display: flex;
    align-items: center;
  }
`
